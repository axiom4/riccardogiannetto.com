import { ImageLazyLoaderDirective } from './image-lazy-loader.directive';

describe('ImageLazyLoaderDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageLazyLoaderDirective();
    expect(directive).toBeTruthy();
  });
});
